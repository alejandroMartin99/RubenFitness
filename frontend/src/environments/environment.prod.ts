/**
 * Environment Configuration
 * Production environment settings
 */

export const environment = {
  production: true,
  apiUrl: '', // Update with production API URL
  supabaseUrl: '', // Update with production Supabase URL
  supabaseKey: '', // Update with production Supabase key
  openaiApiKey: '', // Never expose in frontend
  firebaseConfig: {
    // Add production Firebase configuration here
  }
};


